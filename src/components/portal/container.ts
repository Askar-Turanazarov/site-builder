/**
 * Контейнер оформления продукта: шапка и подвал портала, секции главной,
 * витрина шаблонов, полоса демонстрации и полоса администратора.
 *
 * Раньше здесь стоял `max-w-6xl` (1152 px), и на экране ноутбука 1920 px по
 * бокам оставалось по ~380 px пустоты. 1440 px держат строку читаемой и при
 * этом используют ширину экрана.
 *
 * Контейнер блоков собранного сайта (`CX.container` в src/blocks/classes.ts)
 * сюда намеренно не входит: ширина сайта — решение шаблона и его владельца.
 */
export const PORTAL_CONTAINER = "mx-auto w-full max-w-[1440px] px-5 sm:px-8 xl:px-12";
